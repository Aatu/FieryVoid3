import THREE from "three";

const HEX_SIZE = 25;
const ZOOM_MAX = 40;
const ZOOM_MIN = 0.05;

const COLOR_FRIENDLY = new THREE.Color(39 / 255, 196 / 255, 39 / 255);
const COLOR_ENEMY = new THREE.Color(196 / 255, 39 / 255, 39 / 255);

export { HEX_SIZE, ZOOM_MAX, ZOOM_MIN, COLOR_FRIENDLY, COLOR_ENEMY };
